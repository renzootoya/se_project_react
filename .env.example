# Frontend environment variables
# Copy this file to .env and fill in your values

# URL of the backend API (used during local development)
# In production (Railway), the frontend is served by the same server, so this is not needed
REACT_APP_API=http://localhost:3001/api
